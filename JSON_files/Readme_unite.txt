Readme.txt

unite.shを実行すると、EL_DeviceDescription_x_x_x.jsonとlist.txtが作成される。
実行方法は、ターミナルでこの directory に cd したのち、以下を実行する。

> ./unite.sh

いずれかのjson fileを修正した場合、unite.shを実行してEL_DeviceDescription_x_x_x.jsonを更新すること。